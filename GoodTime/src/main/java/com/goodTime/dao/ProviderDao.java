package com.goodTime.dao;

import com.goodTime.domain.Provider;

public interface ProviderDao {

	public void registerProvider(Provider provider);
	
	//public String loginProvider(Provider provider);
}
